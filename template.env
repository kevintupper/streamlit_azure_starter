# MSAL Settings

AZURE_CLIENT_ID="<Your Azure AD app's client ID>"
AZURE_CLIENT_SECRET=Your Azure AD app's client secret.
- **AZURE_TENANT_ID** = `"common"` for multi-tenant or your specific tenant ID.
- **REDIRECT_URI** = URL that Azure AD redirects to (often `http://localhost:8501`).
- **ALLOWED_TENANTS** = Comma-separated list of tenant IDs that are allowed to log in.
